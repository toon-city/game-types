{
  "name": "@toon-live/game-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for toon-live (server, socket, web)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
